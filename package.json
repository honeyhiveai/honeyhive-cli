{
  "version": "1.0.0-rc.7",
  "name": "@honeyhive/cli",
  "type": "module",
  "description": "HoneyHive CLI",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/honeyhiveai/honeyhive-cli.git"
  },
  "homepage": "https://honeyhiveai.github.io/honeyhive-cli/",
  "main": "dist/index.js",
  "bin": {
    "honeyhive": "dist/index.js"
  },
  "//": "This package is published publicly, so its runtime deps cannot use pnpm's `catalog:` protocol. `commander`'s version range here MUST match the one in @hive-kube/server-sdk-generator's package.json — the generator imports Commander at codegen time to derive `opts` attribute names that get baked into this CLI's generated source. See AGENTS.md.",
  "dependencies": {
    "@honeyhive/api-client": "1.0.0-rc.5",
    "commander": "13.1.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
